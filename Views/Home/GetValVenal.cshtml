@if (Model != null)
{
    <table class="table table-sm table-striped table-bordered m-2">
        <thead>
            <tr>
                <th>Prefecture</th>
                <th>Quartier</th>
                <th>Superficie</th>
                <th>Valeur venale du terrain</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>@Model.Prefecture</td>
                <td>@Model.District</td>
                <td>@Model.Area</td>
                <td>@Model.ValVenal</td>
            </tr>
        </tbody>
    </table>
}
